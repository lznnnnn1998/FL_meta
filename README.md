**Environment**

Install the environment by those commands:

`pip install torch`

`pip install torchvision`

`pip install numpy`

**Train**

Comment out the corresponding attacks or defenses, and run:

`python main.py`


